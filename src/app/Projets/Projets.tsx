import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Projets: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Projets Page Title!</Title>
  </PageSection>
)

export { Projets };
