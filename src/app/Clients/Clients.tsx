import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Clients: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Clients Page Title!</Title>
  </PageSection>
)

export { Clients };
